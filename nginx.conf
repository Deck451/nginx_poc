events {
}


http {
    server {
        listen 81;
        
        location / {
            root   /usr/share/nginx/html;
            index  index.html index.htm;
        }
    }

    include /etc/nginx/conf.d/*.conf;
}
